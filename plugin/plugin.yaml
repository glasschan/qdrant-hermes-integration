name: hermes-memory-qdrant
version: 0.4.1
description: "Qdrant vector memory — semantic long-term memory via local Qdrant + any OpenAI-compatible embedding API."
hooks:
  - sync_turn
pip_dependencies:
  - qdrant-client
